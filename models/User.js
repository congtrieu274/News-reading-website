"use strict";
class User {
  constructor(fistName, lastName, userName, passWord, confirmPassWord) {
    this.fistName = fistName;
    this.lastName = lastName;
    this.userName = userName;
    this.passWord = passWord;
    this.confirmPassWord = confirmPassWord;
  }
}
